

bcd_ball_count = 15;
bcd_bc_to_hb_offset = 2; // ball chain wheel to herringbone wheel offset
//bcd_clearance = 0.25; // basically for wheels and shits

// the herringbone-wheels-parameters.scad are used instead
//bcd_wheel_h = 7;